apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: odic-providers
  labels:
    app.kubernetes.io/component: beeai-ui
    app: beeai-ui
  namespace: default
stringData:
  providers.json: |
    {{- toJson .Values.oidc.nextauth_providers | nindent 4 -}}
