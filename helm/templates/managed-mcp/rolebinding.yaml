apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: managed-mcp
  labels:
    {{- include "agentstack.labels" . | nindent 4 }}
    app.kubernetes.io/component: managed-mcp
subjects:
  - kind: ServiceAccount
    name: managed-mcp
    namespace: {{ .Values.connector.managed_mcp.namespace }}
roleRef:
  kind: Role
  name: managed-mcp
  apiGroup: rbac.authorization.k8s.io
