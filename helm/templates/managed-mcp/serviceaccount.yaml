apiVersion: v1
kind: ServiceAccount
metadata:
  name: managed-mcp
  labels:
    {{- include "agentstack.labels" . | nindent 4 }}
    app.kubernetes.io/component: managed-mcp
automountServiceAccountToken: true
