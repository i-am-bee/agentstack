---
title: "Quickstart"
description: "Get BeeAI running locally in minutes"
---

## Prerequisites

- Basic terminal/command line experience
- LLM API key (free options: [OpenRouter](https://openrouter.ai/), [Groq](https://groq.com/)) or [Ollama](https://ollama.com/) running locally
- Fast internet connection (initial download: 2-3GB)

## Installation

<Tabs>
<Tab title="Linux & macOS">

## One-Line Install

Open the terminal and run this command to install BeeAI:

```bash
sh -c "$(curl -LsSf https://raw.githubusercontent.com/i-am-bee/beeai-platform/refs/heads/main/install.sh)"
```

This interactive script installs BeeAI CLI, downloads and starts the platform, prompts you to configure your LLM API key, then launches the web interface.

## Manual Install

<Steps>
<Step title="Install uv">

Follow the [installation instructions](https://docs.astral.sh/uv/getting-started/installation/).

</Step>
<Step title="Linux only: Install QEMU">

Install QEMU via package manager ([instructions](https://www.qemu.org/download/)).

</Step>
<Step title="Install BeeAI">

Open a new terminal and run:

```bash
uv tool install --refresh --quiet --force beeai-cli && beeai self install
```

Follow the interactive prompts to finish installation and setup.

</Step>
</Steps>

</Tab>
<Tab title="Windows (Experimental)">

Requires Windows 11 22H2+

<Steps>
<Step title="Install uv">

Open PowerShell and run:

<Tabs>
<Tab title="Installation script (recommended)">

```bash
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

</Tab>
<Tab title="Winget">

```bash
winget install astral-sh.uv
uv tool update-shell
```

</Tab>
<Tab title="Scoop">

```bash
scoop install uv
uv tool update-shell
```

</Tab>
<Tab title="Chocolatey">

```bash
choco install uv
uv tool update-shell
```

</Tab>
</Tabs>

Close and reopen your terminal after installation.

</Step>
<Step title="Install BeeAI">

```bash
uv tool install --refresh --quiet --force beeai-cli && beeai self install
```

Follow the interactive prompts to finish the installation and setup.

</Step>
</Steps>
</Tab>
</Tabs>

## Usage

```sh
beeai ui                     # Launch web interface
beeai list                   # See what agents are available
beeai run chat "Hi!"         # Send a message to chat agent
beeai run chat               # Try interactive mode
beeai info chat              # View agent details
beeai --help                 # See all options
```

## Platform Management

```sh
beeai platform start        # Start platform
beeai model setup           # Configure LLM provider
beeai platform stop         # Stop (keeps data)
beeai platform delete       # Delete all data
```

## Version Management

```sh
beeai self version          # Check version
beeai self upgrade          # Update
beeai self uninstall        # Remove completely
```

Explore the rest of the documentation to learn how to connect MCP servers, build your own agents, use document retrieval, and more! 🐝
